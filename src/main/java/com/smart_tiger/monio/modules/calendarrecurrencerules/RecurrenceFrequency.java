package com.smart_tiger.monio.modules.calendarrecurrencerules;

import lombok.ToString;

@ToString
public enum RecurrenceFrequency {
    DAILY,
    WEEKLY,
    MONTHLY,
    YEARLY,
    SAME_WEEKDAY_MONTHLY,
}
